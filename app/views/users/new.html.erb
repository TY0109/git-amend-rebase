<h1>ユーザー作成</h1>
<%= form_with(model: @user, url: users_path, local: true) do |f| %>
  <div class= "form-group">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <%= f.submit %>
<% end %>
  